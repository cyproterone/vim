#!/usr/bin/env bash

set -eux
set -o pipefail


cd "$(dirname "$0")/.." || exit


pip3 install --upgrade --target "vars/pip_modules" -- pynvim
VIM_INIT=1 nvim --headless
nvim --headless +UpdateRemotePlugins +quit
