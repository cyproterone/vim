#!/usr/bin/env bash

set -eu
set -o pipefail


cd "$(dirname "$0")/../vars" || exit


ROOT="pip_modules"
mkdir -p "$ROOT"
PKGS=($(< '../pip.txt'))
pip install --upgrade --target "$ROOT" -- "${PKGS[@]}"
